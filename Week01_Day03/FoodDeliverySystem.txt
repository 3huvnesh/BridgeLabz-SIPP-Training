
Class Customer
    Attributes: name, address
    Constructor(name, address)

Class Restaurant
    Attributes: name, location
    Constructor(name, location)

Class DeliveryBoy
    Attributes: name, phone
    Constructor(name, phone)

Class Order
    Attributes: customer, restaurant, deliveryBoy, foodItem
    Constructor(customer, restaurant, deliveryBoy, foodItem)
        set values
    Method showOrderDetails()
        print "Order Details:"
        print "Customer:", customer.name, customer.address
        print "Restaurant:", restaurant.name, restaurant.location
        print "DeliveryBoy:", deliveryBoy.name, deliveryBoy.phone
        print "Food:", foodItem

Main
    cust = Customer("Alice", "Street 21")
    rest = Restaurant("Pizza Hut", "Downtown")
    boy = DeliveryBoy("Raj", "9876543210")
    order = Order(cust, rest, boy, "Veg Pizza")

    order.showOrderDetails()
